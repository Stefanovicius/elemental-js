{
  "name": "elemental-monorepo",
  "private": true,
  "version": "1.0.0",
  "author": "Adomas Stefanovičius",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "cd packages/lib && pnpm run dev",
    "dev:site": "pnpm run build && cd packages/site && pnpm run dev",
    "build": "cd packages/lib && vite build",
    "build:site": "cd packages/site && vite build",
    "preview:site": "cd packages/site && vite preview"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.12",
    "prettier": "^3.4.2",
    "vite": "^6.0.11"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
