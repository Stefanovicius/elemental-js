{
  "name": "elemental-monorepo",
  "private": true,
  "version": "1.0.0",
  "author": "Adomas Stefanovičius",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter elemental-js dev",
    "build": "pnpm --filter elemental-js build",
    "dev:site": "pnpm --filter elemental-js build:watch & pnpm --filter elemental-js dev & pnpm --filter elemental-site dev & wait",
    "build:site": "pnpm --filter elemental-site build",
    "preview:site": "pnpm --filter elemental-site preview"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.12",
    "prettier": "^3.4.2",
    "vite": "^6.0.11"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
